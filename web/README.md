# Web UI

This is a lightweight Streamlit dashboard for demo purposes.

```bash
streamlit run web/app.py
```

It reads local durable state files such as `.tasks/`, `.scheduled_tasks.json`, `.memory/MEMORY.md`, and `.worktrees/`.
